[package]
name = "incurs-mcp-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT"
description = "Provider-neutral MCP standard profiles and negotiation for incurs"
repository = "https://github.com/douglance/incurs"
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
