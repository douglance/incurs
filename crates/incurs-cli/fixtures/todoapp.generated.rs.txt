// @generated by `incurs gen`; do not edit.

/// Canonical command manifest used to generate this module.
pub const MANIFEST_JSON: &str = "{\"commands\":[{\"description\":\"Add a new todo item\",\"examples\":[{\"command\":\"\\\"Buy groceries\\\"\",\"description\":\"Add with default priority\"},{\"command\":\"\\\"Fix bug\\\" --priority high\",\"description\":\"Add with high priority\"},{\"command\":\"\\\"Read book\\\" -p low\",\"description\":\"Add with short alias\"}],\"name\":\"add\",\"schema\":{\"args\":{\"properties\":{\"title\":{\"description\":\"The todo title\",\"type\":\"string\"}},\"required\":[\"title\"],\"type\":\"object\"},\"options\":{\"properties\":{\"priority\":{\"default\":\"medium\",\"description\":\"Priority level\",\"enum\":[\"low\",\"medium\",\"high\"],\"type\":\"string\"}},\"type\":\"object\"}}},{\"description\":\"Mark a todo as done\",\"examples\":[{\"command\":\"1\",\"description\":\"Complete todo #1\"}],\"name\":\"complete\",\"schema\":{\"args\":{\"properties\":{\"id\":{\"description\":\"The todo ID to complete\",\"type\":\"number\"}},\"required\":[\"id\"],\"type\":\"object\"}}},{\"description\":\"Get a todo by ID\",\"examples\":[{\"command\":\"1\",\"description\":\"Get todo #1\"}],\"name\":\"get\",\"schema\":{\"args\":{\"properties\":{\"id\":{\"description\":\"The todo ID\",\"type\":\"number\"}},\"required\":[\"id\"],\"type\":\"object\"}}},{\"description\":\"List todo items\",\"examples\":[{\"command\":\"\",\"description\":\"List all todos\"},{\"command\":\"--status pending\",\"description\":\"List only pending\"},{\"command\":\"--json\",\"description\":\"Output as JSON\"}],\"name\":\"list\",\"schema\":{\"options\":{\"properties\":{\"limit\":{\"default\":50,\"description\":\"Maximum number of results\",\"type\":\"number\"},\"status\":{\"default\":\"all\",\"description\":\"Filter by status\",\"enum\":[\"all\",\"pending\",\"done\"],\"type\":\"string\"}},\"type\":\"object\"}}},{\"description\":\"Show todo statistics\",\"name\":\"stats\"},{\"description\":\"Stream progress updates (demo)\",\"name\":\"stream\"}],\"version\":\"incur.v1\"}";

fn quote(value: &str) -> String {
    if value.is_empty() || value.chars().any(char::is_whitespace) {
        format!("{:?}", value)
    } else {
        value.to_string()
    }
}

/// Typed CTA helpers for `add`.
pub mod add {
    /// Canonical command name.
    pub const NAME: &str = "add";

    /// Positional arguments for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Args {
        /// Value for `title`.
        pub title: String,
    }

    /// Named options for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Options {
        /// Value for `priority`.
        pub priority: Option<String>,
    }

    /// Renders this typed invocation as a CTA entry.
    pub fn cta(args: &Args, options: &Options) -> incurs::output::CtaEntry {
        let mut parts = vec![NAME.to_string()];
        parts.push(super::quote(&args.title.to_string()));
        if let Some(value) = &options.priority { parts.push("--priority".to_string()); parts.push(super::quote(&value.to_string())); }
        incurs::output::CtaEntry::Simple(parts.join(" "))
    }
}

/// Typed CTA helpers for `complete`.
pub mod complete {
    /// Canonical command name.
    pub const NAME: &str = "complete";

    /// Positional arguments for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Args {
        /// Value for `id`.
        pub id: f64,
    }

    /// Named options for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Options {
    }

    /// Renders this typed invocation as a CTA entry.
    pub fn cta(args: &Args, options: &Options) -> incurs::output::CtaEntry {
        let mut parts = vec![NAME.to_string()];
        parts.push(super::quote(&args.id.to_string()));
        incurs::output::CtaEntry::Simple(parts.join(" "))
    }
}

/// Typed CTA helpers for `get`.
pub mod get {
    /// Canonical command name.
    pub const NAME: &str = "get";

    /// Positional arguments for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Args {
        /// Value for `id`.
        pub id: f64,
    }

    /// Named options for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Options {
    }

    /// Renders this typed invocation as a CTA entry.
    pub fn cta(args: &Args, options: &Options) -> incurs::output::CtaEntry {
        let mut parts = vec![NAME.to_string()];
        parts.push(super::quote(&args.id.to_string()));
        incurs::output::CtaEntry::Simple(parts.join(" "))
    }
}

/// Typed CTA helpers for `list`.
pub mod list {
    /// Canonical command name.
    pub const NAME: &str = "list";

    /// Positional arguments for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Args {
    }

    /// Named options for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Options {
        /// Value for `status`.
        pub status: Option<String>,
        /// Value for `limit`.
        pub limit: Option<f64>,
    }

    /// Renders this typed invocation as a CTA entry.
    pub fn cta(args: &Args, options: &Options) -> incurs::output::CtaEntry {
        let mut parts = vec![NAME.to_string()];
        if let Some(value) = &options.status { parts.push("--status".to_string()); parts.push(super::quote(&value.to_string())); }
        if let Some(value) = &options.limit { parts.push("--limit".to_string()); parts.push(super::quote(&value.to_string())); }
        incurs::output::CtaEntry::Simple(parts.join(" "))
    }
}

/// Typed CTA helpers for `stats`.
pub mod stats {
    /// Canonical command name.
    pub const NAME: &str = "stats";

    /// Positional arguments for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Args {
    }

    /// Named options for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Options {
    }

    /// Renders this typed invocation as a CTA entry.
    pub fn cta(args: &Args, options: &Options) -> incurs::output::CtaEntry {
        let mut parts = vec![NAME.to_string()];
        incurs::output::CtaEntry::Simple(parts.join(" "))
    }
}

/// Typed CTA helpers for `stream`.
pub mod stream {
    /// Canonical command name.
    pub const NAME: &str = "stream";

    /// Positional arguments for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Args {
    }

    /// Named options for this command.
    #[derive(Clone, Debug, Default)]
    pub struct Options {
    }

    /// Renders this typed invocation as a CTA entry.
    pub fn cta(args: &Args, options: &Options) -> incurs::output::CtaEntry {
        let mut parts = vec![NAME.to_string()];
        incurs::output::CtaEntry::Simple(parts.join(" "))
    }
}

