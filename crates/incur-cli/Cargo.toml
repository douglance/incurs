[package]
name = "incur-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "incur"
path = "src/main.rs"

[dependencies]
incur = { path = "../incur", features = ["cli"] }
tokio = { version = "1", features = ["full"] }
